@inject NavigationManager Navigation

@code
{
	protected override void OnInitialized()
	{
		var returnUrl =
			Uri.EscapeDataString
			(stringToEscape: Navigation.Uri);

		var uri =
			$"authentication/login?returnUrl={returnUrl}";

		Navigation.NavigateTo(uri: uri);
	}
}
